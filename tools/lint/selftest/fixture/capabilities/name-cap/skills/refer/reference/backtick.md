# Backtick sibling

The nested-reference violation written the way authors actually write it: see
`reference/deep.md` for the details. No markdown link, so a check that only matched
`](x.md)` could not see this at all — and the truncation it causes is identical.

Not violations, and they must stay that way: this file is `reference/backtick.md`
(itself), and `reference/naming.md` belongs to a different skill.
