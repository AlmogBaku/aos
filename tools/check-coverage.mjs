#!/usr/bin/env node
// Stub — the real checks land in Task 2 of the ripple plan. Exits 0 so check.sh's wiring is
// verifiable now, while the retired-token gate beside it is deliberately red.
console.log('coverage gate: not implemented yet (stub)');
