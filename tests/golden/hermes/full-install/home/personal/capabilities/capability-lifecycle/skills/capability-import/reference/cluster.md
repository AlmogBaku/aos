# Stage 2 — Cluster

Group the inventory into candidate use cases. Signals: shared names/prefixes, prompt
cross-references, shared KB paths, shared secrets, delivery targets.

Present the cluster implementing the use case the user named, plus an "adjacent,
excluded" list. Let the user redraw the boundary before mapping. Ambiguous membership is
a question, not a guess.
