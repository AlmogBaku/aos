# Build-gap ledger

Append-only. Every mismatch the build phase finds between an artifact and the spec gets a row.
The spec fix lands **in the same commit** as the artifact that revealed the gap (firm-position
discipline, ARCHITECTURE §8). Anything in an open RFC's territory is appended to that RFC as
evidence — never quietly fixed in ARCHITECTURE. A proposed new frontmatter field must name its
two in-repo machine consumers (rule of two) or stay prose.

| Date | WP | Spec section | Mismatch | Resolution |
|---|---|---|---|---|
| 2026-07-22 | WP3 (pre-seeded) | design/kb-methodology.md §8 vs ARCHITECTURE §2.1 | `archiver.agent.yaml` placed inside the methodology dir by kb-methodology §8, but §2.1 and kb-authorization put agent specs in `agents/` (where `schedules[].agent` resolves) | **fixed in kb-methodology §8**: canonical in `agents/`, prompts in `methodologies/*/archiver/` |
| 2026-07-22 | WP3 (pre-seeded) | ARCHITECTURE §2.1 / §3.1 | ONBOARDING.md question `type` vocabulary is never enumerated anywhere | **resolved in artifacts**: `string\|number\|boolean\|enum\|list\|path` + orthogonal `secret` (lint enforces; normative text = onboarding interview skill) |
| 2026-07-22 | WP4 | ARCHITECTURE §2.2 vs design/capability-anatomy.md | §2.2's example `prompt_ref: skills/capture/drain-prompt.md` contradicts the anatomy exhibit (`skills/drain/drain-prompt.md` — the prompt belongs to the drain skill) | **fixed in §2.2** (anatomy wins) |
| 2026-07-22 | WP4 | capabilities/kb + gtd-capture | Both kb `nightly-promote` and gtd `nightly-drain` defaulted to 23:00 on one inbox — undefined order | **resolved in artifacts**: drain triages at 23:00 and only marks `#triaged`; promote files + empties at 23:30. Ordering documented in both manifests |
| 2026-07-22 | WP3 | ARCHITECTURE §2.2 | Methodology-shipped skills (karpathy-3layer `lint/`) can't appear in manifest `skills[]` (path fixed to `skills/<id>/`), so they have no `used_by` declaration | **documented in kb-methodology §8**: the methodology contract carries them; they materialize with the archiver. Rule-of-two: no new manifest field |
| 2026-07-22 | WP6 (pre-seeded) | ARCHITECTURE §2.1 | Is `MOD.example.md` required when a capability has no ONBOARDING.md (importer)? | **resolved in artifacts**: presence-paired — required iff ONBOARDING.md exists (lint `manifest/mod-example`); importer ships neither |
| 2026-07-22 | WP5 (routing replay) | design/kb-authorization.md §2.1 / ARCHITECTURE §4.2 | "Explicit tag wins" but nothing stores the tag→KB mapping ("work:" names no KB literally) — step 1 was under-determined | **resolved in artifacts**: registry entries carry `tag:` alias (init skill shape + fixture); route skill matches prefix against `tag`/`name`. Registry shape is RFC-006 input — noted there via this row |
| 2026-07-22 | WP2 | ARCHITECTURE §5.3 | Hermes agent row claimed "profile + entry in config.yaml" — profiles are directory-defined; no config.yaml registry exists | **fixed in §5.3** (same commit as the Hermes cheatsheet); create via `hermes profile create` |
| 2026-07-22 | WP2 | ARCHITECTURE §5.3 | Hermes schedule row said write `jobs.json` "tagged `origin: aos:<cap>@<ver>`" — jobs.json is scheduler-owned (live state, schema drift) and its `origin` field already means chat provenance | **fixed in §5.3**: `hermes cron create` + `aos:<cap>:<id>` name prefix + lockfile job id |
| 2026-07-22 | WP2 | ARCHITECTURE §3.1, §5.3 | Secret store named `auth.json`/`hermes-auth` — auth.json is Hermes's provider-credential state; capability secret values live in `.env` | **fixed** (§3.1 example → `{store: hermes-env, key: VAR}`; §5.3 secret row) |
| 2026-07-22 | post-build review | ARCHITECTURE §5.2 | Host vocabulary said `scheduler`; every harness's actual noun is `cron` (hermes cron, openclaw cron, Claude Code CronCreate, opencode-cron plugins — researched) | **renamed to `cron`** on spec branch + all main artifacts (lint vocabulary, manifests, cheat-sheet, BOOTSTRAP) |
| 2026-07-22 | post-build review | ARCHITECTURE §4.4 / design/kb-methodology.md | Methodology named `karpathy-3layer` — an invented label; the pattern's public name is karpathy's llm-wiki (gist 442a6bf) | **renamed `karpathy-llm-wiki`** on both branches (dir, manifests, registry values, spec docs); ledger keeps historical rows verbatim |
| 2026-07-23 | kb redesign | ARCHITECTURE §2, §4.4 (spec c8ef7a3) | kb redesigned around the base engine (store/curation/state): methodology seam dissolved, entry-skill convention, `base` tool as canonical [D] executor, exec schedules, BASE.yaml, current-truth doctrine, inbox-as-view, state.yaml. Spec amended first (firm-position discipline); artifacts rebuilt against it | **this commit**: capability tree v2 (skills kb/route/recall/init/adopt; agents/archiver + prompts; templates; tool), lint dual-anatomy, expectations updated. Other capabilities NOT migrated (deliberate — `_migrate.md` local ledger; lint tolerates legacy with warning) |
| 2026-07-23 | kb redesign | ARCHITECTURE §2.2 | Schedules could not express script-direct crons — kb-sync woke an LLM every 5 min to run a shell script | **spec + artifacts**: `schedules[].exec` (xor agent+prompt_ref), lint-enforced; kb `sync` schedule is exec-type; cheat-sheet materialization documented |
| 2026-07-23 | kb redesign | design/kb-methodology.md §4 vs §5.3 | Spec's universal frontmatter block omitted `growth_stage` while §5.3 keeps growth stages | **resolved in artifacts** (optional universal field, tool-linted when present); spec §4 should list it as optional — noted for next spec touch |
| 2026-07-23 | kb redesign | RFC-009 (opened) | Cross-capability skill dependency has no vocabulary (`used_by` can't cross capabilities); gtd-capture→kb composes only via shared main agent | **RFC-009 opened on spec branch**; deferred to its own session per user |
| 2026-07-23 | kb redesign | capabilities/gtd-capture | gtd drain reads `ops/inbox.md`, which the redesigned base no longer has (inbox = pending view over raw/captures/) | **deferred to gtd-capture migration** (`_migrate.md` row): drain consumes `base inbox`; zone grant becomes `route-into raw/captures/**` |
| 2026-07-24 | import + LFS | design/kb-methodology.md §2, §6.7, §9 (spec 249596b, amended same-day) | Import initially specced/built with a deterministic apply engine (plan DSL + transform queue); user correctly flagged it as over-engineered — transform-on-import routes every page through agent judgment anyway, so the engine served the rejected lift-and-place mode | **reversed in artifacts + spec amended**: import is an AGENT procedure (skill + agreement file + progress checklist + subagent batches); the tool keeps exactly one import verb, `survey`. LFS landed as standalone base plumbing (gitattributes template, init wiring, lint check) |
| 2026-07-24 | anatomy migration | ARCHITECTURE §2.5, `_migrate.md` (replay ledger, deleted this commit) | `onboarding`, `gtd-capture`, `importer` were still on the pre-redesign anatomy (no entry skill, flat CAPABILITY.md, `sections/` dirs) while `kb` had already moved | **resolved in artifacts**: all three migrated to the entry-skill convention (`skills/<id>/SKILL.md` + `reference/`), CAPABILITY.md restructured to the installer's-briefing skeleton, `tools/lib/constants.mjs`'s `LEGACY_ANATOMY_CAPS` emptied (the `structure/entry-skill` lint check is strict for every capability now) |
| 2026-07-24 | anatomy migration | capabilities/gtd-capture, RFC-009 (open) | Row 27's deferred item (`ops/inbox.md` no longer exists) plus RFC-009's open question (does gtd-capture's drainer need a foreign skill from kb?) both landed on this migration | **resolved in artifacts**: `drain` now consumes `base inbox` / `base inbox --failed` instead of a flat file; the old `ops/inbox.md` zone is dropped outright (kb is infra — gtd-capture builds on kb's already-declared zones rather than inventing its own); standalone next-actions get a grant into kb's *existing* `_ops/` zone (`_ops/next-actions.md`, owner_agent: drainer), not a new top-level dir. RFC-009 itself is **not resolved** — only its evidence question is answered: the drainer composes with kb entirely through the `base` tool on PATH (a shell command), never through `used_by` skill-sharing, so no cross-agent skill need actually surfaced. Evidence recorded on the RFC (spec branch); the RFC stays open |
| 2026-07-24 | anatomy migration | capabilities/importer | `_migrate.md`'s importer row claimed "imports should enter via `base capture`/catalog verbs instead of direct raw/ writes" — this turned out to describe kb's own new bulk-import feature (`capabilities/kb/skills/import/`), not this capability; `capabilities/importer` never wrote to any KB's `raw/` at all (it only emits a draft capability skeleton + `MOD.md` + `GAP.md`) | **no behavior change needed**: importer's inventory→cluster→map→split→emit pipeline is untouched; only its anatomy migrated (entry skill, `reference/` rename). The ledger's claim is noted here as a correction, not carried forward |
| 2026-07-24 | docs pass | design/install-flow.md §1 | `BOOTSTRAP.md` moved to the clone root (the agent's entry point beside the README — the paste-block gets a shorter, more robust path); the spec's §1 quoted the paste-block with the old `docs/BOOTSTRAP.md` path | **resolved both sides**: main sweep (README paste-block, DOGFOOD, PROTOCOL, onboarding README, CLAUDE.md; historical transcripts/ledger rows verbatim); spec-side `design/install-flow.md` §1 edited to the root path. `docs/diagram.svg` also copied from the spec branch onto main by explicit decision (README/CONCEPTS illustration — an image asset, not spec text; the no-copy rule keeps applying to spec *documents*) |
| 2026-07-24 | capability-builder | ARCHITECTURE §7 (build order), new §9 | `capability-builder` built outside the original eleven-capability reference set, at the maintainer's direction — the spec had no section covering a building-mode boundary (no one-pager, no §7 row, nothing in §8 on a capability that authors capabilities) | **spec amendment drafted on the `spec` worktree, same day (lands as its own spec commit)**: new §9 (capability-authoring mode), §7 row 12 + appended-not-resequenced note, §8 firm position (building mode = prompt-enforced mode-switch on `main`, approval-gated; not a materialized agent), one-pager `capabilities/capability-builder.md`. Artifacts land on main in this commit; no new manifest fields (rule of two untouched) |
| 2026-07-24 | install funnel | ARCHITECTURE §5.1–§5.3, design/install-flow.md §1 (spec 252b0c0) | The funnel leaked harness plumbing to the human and dead-ended unlisted harnesses: the paste-block made the user name their harness's cheat-sheet path, BOOTSTRAP hard-stopped when no cheat-sheet existed ("do not improvise"), the sheet was loaded up-front rather than at the steps that use it, and every sheet restated the aos-side install rules | **spec amended (252b0c0, same day)**: §5.2 gains the three usage rules — aid never gate (no-cheat-sheet installs follow BOOTSTRAP's generic mapping contract and may self-draft a sheet, diff-gated), loaded per operation not standing context, lean (aos invariants live once in `BOOTSTRAP.md` §0, never restated per harness); install-flow §1 paste-block is BOOTSTRAP-only. **main**: README/INSTALL paste-blocks and PROTOCOL e2e prompt bootstrap-only; BOOTSTRAP §0 grew into the shared install contract + a final no-cheat-sheet procedure; `hermes.md` slimmed to the Hermes half |
| 2026-07-24 | install funnel | ARCHITECTURE §5.1 diagram, §5.2, §1 species list; design/capability-anatomy.md; spec CLAUDE.md (spec 252b0c0) | Cheat-sheet path was `harnesses/<harness>/CHEATSHEET.md` — a directory per harness for one file, and `<harness>` never defined (§1.1's "no runtime" made "your harness" ambiguous for the installing agent) | **renamed** to flat `harnesses/<harness-runtime>.md` on both branches (concept name "cheat-sheet" unchanged); §5.2 defines *harness runtime* by example (Hermes → `harnesses/hermes.md`, … — the runtime is the harness's, never the kit's); lint regex + selftest fixture moved; `nanoclaw.md` (one sheet covering v1+v2), `openclaw.md`, `nanobot.md` added research-drafted per §5.3's honesty note |
| 2026-07-24 | capability-lifecycle | ARCHITECTURE §5, §7 (spec be50bdb) | After bootstrap, nothing in the harness knew how to install — `install <capability>` knowledge lived only in the clone's BOOTSTRAP.md (no skill triggers on it); the lockfile had no owner (contract scattered in prose; the e2e showed the LLM hand-computing 158 sha256s — RFC-004's named reopen evidence); no actor verified prerequisites; bootstrap had no defined user experience | **spec amended (be50bdb, same day)** then **built this commit**: the lifecycle is capability 13 — five main-scoped skills (capability-lifecycle/-installer/-upgrader/-remover/-evolver), the aos-side contract in its `reference/contract.md`, cheat-sheets moved into `capabilities/capability-lifecycle/harnesses/`, the `aos-lock` tool owns manifest parsing + the lockfile (verbs only, never hand-edited), BOOTSTRAP is a warm stub (welcome-first experience, git+uv prerequisites — uv now hard-required, one inline install, hand over) |
| 2026-07-24 | capability-lifecycle | ARCHITECTURE §3.3/§3.4, design/install-flow.md §3 (spec be50bdb) | Upgrade was specced as a three-way LLM merge (new template × current install × MOD.md) while §3.3 simultaneously claimed the render is "always reconstructible from (original × MOD.md)" — the merge input contradicted the reconstructibility claim | **the ledger model (spec amended)**: MOD.md is a ledger of personalization the agent re-applies — upgrade = fold uncaptured hand-edits into the ledger (lockfile-hash detection) → re-render fresh upstream × MOD.md → diff gate; the current install is a drift source, never a merge input. `capability-evolver` is the ledger's named write path (absorbs sync-mod); capability-builder's skill renamed `capability-source-evolver` (shipped source vs overlay boundary, both route to each other) |
| 2026-07-24 | capability-lifecycle | ARCHITECTURE §2.1, §2.5 (spec be50bdb) | §2.1's tree never listed `tool/` although §2.4/RFC-004 sanctioned it (kb's tool leaned on prose); generic skill ids (`install`, `evolve-capability` vs a lifecycle `evolve`) were ambiguity traps in a crowded harness; cross-skill `../` paths break at materialization (dirs renamed to `<capability>-<id>/`) — the spec's own anatomy exhibit carried one | **spec amended**: §2.1 tree gains `tool/` + the lifecycle `harnesses/` exception; §2.5 adds skill-ids-bear-meaning + name-based cross-skill references (anatomy exhibit fixed); §2.2 reserves `x-*` as the manifest extension namespace. **main**: new lint `skill/no-cross-path` bans `../` in shipped skill md (planted fixture); cheat-sheet section check covers the in-capability path |
| 2026-07-24 | capability-lifecycle | ARCHITECTURE §5.2, §9 (spec, same day) | "Plan mode" (a read-only staging mode: enter to research/stage, exit only through an approval gate) was an unmapped concept — STAGE→GATE→EXECUTE and capability-builder's §9 mode boundary both instance it, but no cheat-sheet row said how a harness expresses it | **spec amended + artifacts**: §5.2's Primitive-mapping bullet gains `plan mode`; §9 notes the builder rides the native mode where one exists; all four sheets gain the row (all four: none native — prompt-enforced); capability-builder 0.3.0 enters plan mode at intake and exits only at the Design approval gate; contract.md ties STAGE to the native mode where present |
| 2026-07-25 | one way of working | ARCHITECTURE §3.3, §9, §8 (spec 112e77c) | The overlay ledger had no exit side — a generally-useful MOD nuance could only be re-invented by hand as a source change; nothing defined retirement when upstream absorbs a line; contribution mechanics lived only in repo-facing CONTRIBUTING prose, not as skill knowledge the offering agent loads; and nothing governed when an agent may write to upstream at all | **spec amended (112e77c)**: §3.3 promotion+retirement clause (§3.1 invariant rules verbatim-untouched), §9 promotion judgment (signal-gated, lightest rung, "one user's need is a MOD line; two users' need is a knob") + the hard never-write-upstream-without-approval invariant; §8 rows sharpened. **main**: evolver signal-gated promotion + retirement in the upgrader, overlay.md carries the full judgment framework, source-evolver gains the promotion intake + `reference/contribute.md` (ledger search, issue-first, PR path), builder's judgment-gated contribute question, CONTRIBUTING "one way of working" + agent-PR policy + DCO |
| 2026-07-25 | household layout | ARCHITECTURE §3.1, §3.4, §5, §1.1; RFC-005 (spec 112e77c) | Overlay files as untracked clone residents had no durability (git clean footgun), no home for private capabilities, no sync story; renders were uncommitted despite the transform being non-deterministic; installs copied per harness (no canonical artifact); the lockfile lived inside the clone; batteries-included wording made any second source root sound out of scope | **spec amended (112e77c)**: the household (`~/aos/{upstream,personal,.aos}`), pinned renders as the transform's lock, symlink-only installs (§5.3, NanoClaw ro-mount), household lockfile, §1.1 local-source-roots clarification, RFC-005 resolved (proposed, closes after dogfood). **main**: BOOTSTRAP/README fork-and-clone + household creation, contract/skills/cheat-sheets rewritten, aos-lock `--home` + link records + source roots (tests extended), kb/onboarding/importer path sweep + bumps, pre-household migration offer in the upgrader |
| 2026-07-25 | location doc fixes | ARCHITECTURE §3.1, §5.3; design/capability-anatomy.md (spec 112e77c) | §3.1's secrets example named harness credential state (`auth.json`/`credentials/`/`data/env`) contradicting §5.3 and all four cheat-sheets (values live in `.env`-family stores); §5.3's table wrote bare `skills/<id>/` for three harnesses where the contract + goldens require `<capability>-<id>`; the backup story (`.aos/backups/` vs harness-native snapshots) was never reconciled | **spec amended (112e77c)**: §3.1 secrets example fixed; §5.3 link names carry the prefix on all four harnesses; backup story superseded — `personal/` git history is the primary safety net (§3.4, §5.4), harness-native backups additive; anatomy exhibit re-drawn on the household |
| 2026-07-25 | household e2e | capabilities/onboarding | The live household e2e caught it: onboarding's briefing never told the installer to materialize the **global context block** — the interview wrote `personal/MOD.md` but the front agent's `SOUL.md` got nothing, so identity facts (timezone, sacred time, red lines) were invisible at runtime. The golden sentinel (`aos:onboarding` in `front:SOUL.md`) had passed historically only because earlier runs improvised it | **fixed in artifacts (onboarding 0.3.0)**: "What you materialize" item 2 now names the context block explicitly (distill the global MOD's identity facts into the front agent's identity file per the cheat-sheet's context-block row, inside `aos:onboarding@<version>` markers). Re-verified live: agent-driven remediation produced the block, `aos-lock verify` clean, `check.mjs --live` 0 failures |
| 2026-07-25 | review round 1 (household) | ARCHITECTURE §2.1, §4.1, §5.3, §5.5, §6; design/kb-authorization.md, design/capability-anatomy.md | Adversarial review of the household change found the sweep incomplete on both branches: §2.1 still specified the retired copy-and-rename materialization; §4.1/§6 and kb-authorization still rooted overlay files and importer drafts in the (now pristine) clone; §5.3/§5.5 and the anatomy exhibit described a lockfile `aos-lock` does not write (per-harness dimension, link hashes, dir-tree hashes) leaving the single-owner rule unenforceable; `docs/INSTALL.md` still shipped the pre-household paste-block, and three kb runtime artifacts (`skills/kb`, `skills/init`, the tool's `--registry` help) still pointed the registry at the clone root | **fixed both sides**: spec swept (§2.1 render+link wording, §4.1/§6 personal root, §5.3 link records are structural path→target, §5.5 `schedules_owned` + cross-agent check, anatomy lockfile exhibit re-drawn to the real shape); main swept (kb skills/tool help, `kb` "copied whole" → rendered+symlinked, crosspath lint comment) |
| 2026-07-25 | review round 1 (tooling) | capabilities/capability-lifecycle/tool, tools/lint/aos-lint.mjs, capabilities/kb/tool | Five real defects: `--home`/`AOS_HOME` never expanded `~` (a quoted `~/aos` silently created a literal `~` directory and reported success); `verify` reported a *copy* where a link belongs as "MISSING LINK"; link targets were compared as raw strings (an equivalent relative/absolute target read as drift); `rehash` hard-failed (exit 16) when an evolve deleted a render file, leaving the entry permanently stale; `aos-lint --root` was documented and never parsed, so capability-builder's post-build gate silently linted the kit instead of the package it just wrote; and kb's `find_upstream_root` could return the *personal* root (its `capabilities/` heuristic ran before the `.aos/` marker) | **fixed**: `expanduser()` on `--home`/`AOS_HOME`/artifact/link paths; `NOT A LINK (copies are banned)` split from `MISSING LINK`; link targets normalized to absolute at record and verify; `rehash` drops vanished paths with a printed note; `--root` implemented (verified: lints another tree); kb's household discovery keys on `.aos/` first, with `find_household()` shared by both root resolvers. Tests extended; `check.sh` green |
| 2026-07-25 | review round 1 (goldens) | tests/golden/normalize.mjs, tests/golden/check.mjs, tests/golden/expectations | The re-render committed ~1.15M lines of harness skill-hub cache twice (`.hub` was skipped only inside the bundled-manifest branch), and a renamed script (`aos-kb-sync.sh.unused`) escaped text normalization — leaking absolute developer paths into a committed snapshot (self-containment). Snapshot mode also had no way to assert the symlink contract, since snapshots dereference links | **fixed**: `.hub`/`index-cache`/`.bundled_manifest` globally skipped and the snapshot pruned; TEXT pattern accepts a trailing suffix (`.sh.unused`) and the leaked file normalized; `lockfile_links_into_personal` asserts in snapshot mode that every capability records links whose targets sit under `personal/` — the deterministic residue of the §5.3 contract |
| 2026-07-25 | review round 2 (verification) | docs/INSTALL.md, docs/BUILD-GAPS.md | **Process failure, caught by verification**: round 1's batch edit aborted on an assertion before reaching `docs/INSTALL.md`, so the stale paste-block (`clone to ~/aos`) survived — while the round-1 ledger row and commit message both claimed it had been swept. A user pasting it would land the kit at `~/aos` and BOOTSTRAP would then `git init` the personal repo *inside the pristine clone* — the §3.1 violation the household exists to prevent | **fixed**: INSTALL.md paste-block + `uv tool install` path corrected; the round-1 row de-claimed (a ledger that over-claims is worse than a missing row). Lesson recorded: batch edits must verify each target, not trust a partial run |
| 2026-07-25 | review round 2 (tooling) | capabilities/capability-lifecycle/tool, tools/lint | Verification found round 1's `--root` implemented but *unusable for its only documented purpose* — linting a personal root always errored (dependencies resolve only inside the linted root; the overlay family is banned there), so capability-builder's post-build gate was unsatisfiable; link-target normalization was asymmetric (relative targets `resolve()`d, absolute ones left literal → false RELINKED drift under a symlinked household); `rehash` could empty an entry and `verify` then called it clean; `sha256()` followed symlinks, contradicting BOOTSTRAP's stated contract; and `<home>` had no runtime resolver despite the contract citing one | **fixed**: lint gained personal-root semantics (`depRoots` resolves dependencies into the kit, overlay ban is upstream-only, version-bump skipped off-kit) — verified 0 errors on a realistic personal root with overlay files + a cross-root dependency; `link_target()` normalizes both spellings lexically (no parent-following); `rehash` refuses to empty an entry (exit 16); `--artifact` rejects symlinks before resolve; new `aos-lock home` verb prints the resolved household. Four regression tests added (34 total, green) |
| 2026-07-25 | review round 2 (procedures) | capability-lifecycle skills, contribute.md, tests/golden/PROTOCOL.md | Procedure defects a literal reader would hit: the upgrader treated *link damage* as foldable hand-edit drift; the remover had no per-harness path though the installer has one (removing from one harness would nuke the shared render); `no-cheatsheet` told the agent to switch back to `main` *before* using the draft it had just written on a branch; `gh repo fork` was outside the never-without-approval enumeration despite being an outward write; the golden's sync-script sentinel tested debris left by a previous run | **fixed**: upgrader splits artifact-drift (fold) from link damage (re-link, or stop); remover gained a per-harness scope step; no-cheatsheet reordered (use the draft, switch back after); fork yes-gated in the contribute path (round 3 completed the enumeration in the contract, source-evolver, and spec §9); PROTOCOL requires clearing `aos-*` leftovers in pre-state |
| 2026-07-25 | review round 3 (verification) | capability-lifecycle skills + reference, BOOTSTRAP.md, docs/INSTALL.md; ARCHITECTURE §2.1, §2.2, §9, Appendix B | Final pass found the diff gate — the product's central safety promise — unable to show what it most needed to: `git diff` hides *untracked* files, so a re-render that **adds** a skill or a `reference/` page (the commonest upgrade shape) passed the gate invisibly, and the decline path (`checkout -- .`) left those additions on disk for the next persist commit to sweep in. Also: `no-cheatsheet` never committed the draft it claimed the branch kept (leaving an untracked file in the pristine clone and failing the contribution preflight); "restore on a new machine = relink from the lockfile" was impossible (the lockfile is machine-local); per-harness removal skipped the dependents check; spec §2.1 still created the user's MOD.md inside the shipped tree; BOOTSTRAP's pre-household migration was an illegal single `mv` | **fixed**: the gate stages first, then diffs staged (round 4 scoped every command to the capability — see the next row); the decline restores and cleans that capability only; the cheat-sheet draft is committed on its branch; restore is clone+clone+re-install (INSTALL.md's copy of that claim was missed here and fixed in round 4); per-harness removal keeps a harness-scoped dependents check and identifies jobs by introspection (the lockfile has no harness dimension); §2.1 points at the mirrored personal path; BOOTSTRAP stages the move. Fork/remote-branch creation added to every invariant list (contract, source-evolver, spec §9), completing what the round-2 row had over-claimed |
| 2026-07-25 | review round 4 (confirmation) | capability-lifecycle contract + upgrader + overlay, docs/INSTALL.md, three invariant summaries | Confirming round 3 found that its own gate fix had **widened the blast radius it claimed to narrow**: the scoped `clean -fd -- capabilities/<id>` sat after an *unscoped* `git add -A` and `git reset --hard HEAD`, so declining an upgrade reverted the user's unrelated tracked edits and deleted their unrelated untracked files in `personal/` — strictly worse than the `checkout -- .` it replaced, under a sentence promising "nothing else of the user's is touched". Its stated rationale was also false (after `add -A`, `reset --hard` does remove the additions, so the `clean` was a no-op). Separately, `docs/INSTALL.md` still carried the impossible "clone + clone + relink" restore — the third consecutive round that file escaped a sweep | **fixed and proven in a scratch repo**: every gate command is path-scoped — `add -A -- capabilities/<id>` → `diff --staged -- capabilities/<id>`; decline is `restore --staged --worktree -- capabilities/<id>` + `clean -fd -- capabilities/<id>`, which restores the capability exactly while leaving an unrelated modified `kb/MOD.md` and an untracked `kb/NOTES.md` intact. Round 5 corrected the rationale a third time — `restore --staged --worktree` *already* removes staged additions and prunes their directories, so `clean -fd` is scoped belt-and-braces, not a requirement — and documented the one residual: `add -A -- capabilities/<id>` sweeps a pre-existing untracked file of the user's inside that capability dir, which declining would delete, so the skills now check `git status` and say so first. INSTALL.md's restore claim corrected; fork/remote-branch completed in the three summary enumerations |
| 2026-07-25 | MOD semantics | ARCHITECTURE §3.3 (spec); capability-lifecycle skills + reference, capability-builder, docs | User correction: **MOD.md is not a ledger.** It specifies what the user changed from the shipped defaults — *current desired state* — not an append-only record. The write rule contradicted this for prose: typed answers were edited in place (correct) but nuances were appended as "a dated imperative line", so changing office hours from "Thursday 17:00–18:00" to "none" produced two contradicting statements instead of one edited (or deleted) one. Dates on MOD entries also duplicated what `personal/`'s git log already holds — the pinned-render design made the append-log redundant without anyone noticing | **fixed both branches**: §3.3's normative definition and every artifact now say MOD *specifies deltas, edited in place* — one statement per subject, rewritten when it changes, **deleted when the setting returns to the shipped default**, added only when nothing covers the subject; no dates on statements (git history carries them). `capability-evolver` reads the current MOD before classifying and routes to edit/delete/add explicitly; the "ledger" noun is gone from every MOD context (KB `## Timeline` and the upstream demand-ledger keep it — both genuinely append-only) |
| 2026-07-25 | skill identity | ARCHITECTURE §2.1, §2.2, §2.5, §5.3, §5.4/§5.5, Appendix B; design/capability-anatomy.md; capabilities/kb.md; RFC-002 | A skill's identity was three different things and the harnesses disagreed about which counted: the source dir and frontmatter `name` (`init`), the installed dir (`kb-init`, per the contract's `<capability>-<id>` rule), and the render's frontmatter, which "stays as shipped" — so the committed goldens carry `name: init` inside `skills/kb-init/`. OpenClaw keys on the frontmatter name (the skill lands as bare `init`), Nanobot required a per-harness rewrite of it, and Hermes's `cron --skill <id>` left which spelling undefined. Nothing checked uniqueness anywhere: `checks/skills.mjs` is per-capability, and there was no install-time or lockfile check, so a second capability shipping one name silently overrides the first. §2.1's own rule ("skill ids must be self-descriptive out of context") was contradicted by `capabilities/kb.md`, which pins `init`/`import`/`adopt`/`route`/`recall` | **spec amended (ad86f6a)**: §2.1/§2.5 — ids are capability-local, the *installed* name (`<skill_prefix><id>`; entry skill = capability id) is the shipped identity, must be globally unique, and is what cross-skill references name; §2.2 — the `skill_prefix` field (rule of two: `capability-lifecycle` and `gtd-capture` declare it, `kb` exercises the default; read by both lint and `aos-lock`); §5.3 — installed name on all four harnesses, Nanobot's rewrite exception dropped; §5.4/§5.5 — a skill-name single-owner rule and gate beside the schedule one, plus a `doctor` duplicate check; Appendix B — a collision risk row; `capabilities/kb.md` + the anatomy exhibit re-drawn on installed names; RFC-002 tier-1 list gains the new checks. **main**: `aos-lock` gains `skills [--check --harness-skills]` (exit 17: household, lockfile links, and the harness's own skills) and `render` (deterministic copy + name rewrite + origin stamp, idempotent); five new lint codes (`skills/prefix-format`, `skills/prefix-redundant`, `skills/installed-name`, `skills/installed-collision`, `skills/ref-unqualified`) with planted fixtures; the contract, install/upgrade skills, all four cheat-sheets, and BOOTSTRAP rewritten onto installed names; `reference/naming.md` carries the rules at runtime |
| 2026-07-25 | four capabilities, one subject | ARCHITECTURE §7, §8, §9 (firm position), Appendix A; capabilities/{onboarding,importer,capability-builder}.md | `capability-builder`, `importer`, and `onboarding` were carving one subject with `capability-lifecycle`. The authoring skills share an invariant word for word — read-only on the live harness, write-only into a draft under `personal/capabilities/<id>/`, never install, never open the PR (`capability-builder/SKILL.md:60`, `reference/build.md:11,20`, `importer/…/emit.md:17`, `capability-builder/CAPABILITY.md:74,85`) — and build's mechanism/nuance split is import's in reverse. Four briefings, four READMEs, and four lockfile entries described one flow | **spec amended (ad86f6a)**: §9 changes a **firm position** — building mode stays a mode boundary but stops being a *package* boundary. Counter-proposal recorded there: the boundary's teeth are the push-context `SOUL.md` block plus the detector skill (found in the first live e2e), and both survive intact; the honest cost is that the boundary now reaches consume-only users, since BOOTSTRAP installs this capability for everyone. §7/§8/Appendix A and the README reading list collapse; the three one-pagers merge into `capabilities/capability-lifecycle.md`. **main**: nine skills under `skill_prefix: capability-`, action-oriented ids (`install`, `onboard`, `build`, `contribute`, …); the capability now ships the *global* `ONBOARDING.md` while its own skills stay `{{mod}}`-slot-free, which is what keeps BOOTSTRAP's inline install mechanical; two discriminated context blocks (`aos:<capability>:<block-id>@<version>` — the marker vocabulary grows the way schedule names already did) |
| 2026-07-25 | Agent Skills conformance | Anthropic's skill-authoring guide (platform.claude.com/docs/…/agent-skills/best-practices) | Audited every shipped skill against the guide. Five real gaps: three descriptions carried XML-ish tags (`"install <capability>"`, `<time>`) although both `name` and `description` are injected into the system prompt; three `reference/` files linked to a *sibling* reference (a file reached through a file gets previewed, not read whole); two >100-line reference files had no Contents block; the reserved words `anthropic`/`claude` were unchecked in `name`; and the ≤64/charset/reserved limits were applied to the source id rather than the *installed* name that actually ships | **fixed on main** (no spec change — the guide is upstream, not ours to amend): all five repaired, and each is now enforced — `skill/xml-tags`, `skill/nested-reference`, `skill/reference-toc`, `skill/reserved-word`, `skills/installed-name`. Already conformant and left alone: name/description limits, third person, trigger phrasing, <500-line bodies, one-level `reference/`, the frontmatter key allowlist, forward slashes, copyable checklists, scripts-executed-never-loaded |
| 2026-07-25 | skill-creator, by reference | — (adoption decision, no spec contract) | The kit had no answer for generic skill craft — drafting a skill, tuning a description for triggering, evaluating one — and the guide's testing checklist (≥3 evaluations per skill, a Haiku/Sonnet/Opus matrix) has no equivalent in tiers 0–2 (unit, lint, a single-model golden e2e) | **main**: `capability-lifecycle` installs Anthropic's `skill-creator` (Apache-2.0) **by reference, never vendored** — the harness's plugin mechanism where one exists (the repo ships `.claude-plugin/marketplace.json`), else a shallow clone in the new `<home>/vendor/` root, symlinked and recorded under this capability so `verify`/`remove`/the name gate treat it as ours. Best-effort: no network or a declined install → say so and continue. Division of authority in `reference/naming.md`: skill-creator owns craft, aos owns identity (prefix, uniqueness, action-vs-role, `used_by`, `{{mod}}`), and aos wins on names. **Still open**: per-skill evaluations and cross-model testing arrive as *referenced knowledge*, not kit machinery — RFC-002 territory, recorded not built |
| 2026-07-25 | host-feature semantics | ARCHITECTURE §5.2, §5.5 | Two live e2e runs read `messaging.inbound: required` two different ways and one of them refused to install. The `depends.host` vocabulary never says whether a feature means "this harness can express it" (the cheat-sheet's Feature notes answer, ✓ for Hermes) or "the user has a channel wired right now" (the `aos-test` profile had none, since it was seeded from a deliberately non-messaging worker profile). A `required` gate that flips on live configuration state blocks installs that would work, and it makes the degraded-mode vocabulary pointless — the whole point of `preferred`/`optional` + `degraded:` is to handle absence gracefully | **fixed on main**: the contract states it once — a `depends.host` feature is a harness capability, answered by the Feature notes table, never by probing configuration; a supported-but-unconfigured channel installs and the user is told what to set up; stop on `required` only when the harness cannot express it at all. `capability-install` step 4 now says the same in the imperative. **spec amended (212724e)**: §5.2 says it in the vocabulary's own definition — the enumerated list describes harness capabilities, and §5.5's degraded modes are what absence-in-practice is for |
| 2026-07-25 | exec-schedule ownership | ARCHITECTURE §2.2, §5.5 | An `exec:` schedule declares no `agent` (that is the point — no LLM wakes up), so nothing said which harness agent hosts the job. Two live e2e runs placed kb's `sync` differently: one in the archiver profile beside kb's other two schedules, one in the front profile. Both "work" — a script job needs no agent — but the golden expectations can only assert one, the removal walk enumerates per profile, and §5.5's single-owner check has to know where to look | **fixed on main**: the contract states it — an exec entry is hosted by the agent that owns the capability's other schedules (the archiver for kb's `sync`), and by the front agent when the capability declares no agent at all; the Hermes sheet's exec row says the same. Placement is bookkeeping, and a capability's jobs staying in one place is what makes the walk enumerable. **spec amended (212724e)**: §2.2's schedule block states it where it defines `exec:` xor `agent`+`prompt_ref` |
| 2026-07-25 | gate robustness | — (main-side defect in this change) | Asked whether the branch is *safer* than main, not just better: the collision gate exempted a capability's own skills using the lockfile's recorded links, and `.aos/` is machine-local and gitignored by design (§3.1). Lose it — new machine, cleared state, a `personal/` clone without it — and every re-install of an already-installed capability hard-stops at exit 17, because the gate reads our own renders as strangers. Verified by reproduction. On main a lost lockfile was recoverable (re-install and re-record); this change would have made it stuck | **fixed on main**: provenance answers when the lockfile cannot — a harness entry is ours if it symlinks into the household or its `SKILL.md` carries `x-aos-origin` (§5.3's attribution rule, which exists for exactly this). Cross-capability collisions are unaffected: both capabilities live in the household, which the source scan reads. Two tests pin both halves — a lost lockfile does not block a re-install, and a genuine stranger holding the name still does |
| 2026-07-25 | identity block vs native user memory | ARCHITECTURE §5.3 (context block row); harness-hermes.md Primitive mapping | User question, and it holds up: the install distills the global MOD's identity facts (timezone, working hours, sacred time, red lines) into the front agent's **`SOUL.md`** — the *agent's* persona file — when Hermes has a native *user-profile* primitive, `memories/USER.md`, that our own cheat-sheet already lists under "files Hermes consumes". Those are different things: SOUL.md is who the agent is, USER.md is what it knows about the user. Worse, it is a second source of truth: built-in memory is "always active" and the agent maintains USER.md from conversation — a spot check found working-hours facts already there, so the two can drift. Inherited from the `onboarding` capability (main writes the same block under `aos:onboarding@…`); this branch only renamed the marker. Complication that makes the obvious fix non-obvious: `hermes memory` exposes only setup/status/off/reset — there is **no sanctioned write path** to USER.md, and its `.lock` file marks it machine-maintained, so appending an aos block there is the config.yaml hazard class (hand-editing a file the harness rewrites) | **decided (user): drop it.** The harness already owns user context — reinventing it was never in scope. Confirmed write-only first: nothing in the kit reads the block, so no behavior depended on it. The clean line is who created the agent — for agents aos creates (`aos-drainer`, `aos-archiver`) the whole identity file is ours, written whole, no markers; the front agent is the user's, and aos does not distil their facts into it. `MOD.md` stays the authoritative store, read at render time. The MARS mode-boundary block stays: it is an instruction about handling requests that create aos artifacts, not a copy of user data, and it is load-bearing (a harness with a native cron tool satisfies a schedule-shaped ask without ever consulting a skill description). `forbid_texts` in the golden expectations asserts the identity marker is absent, and `capability-upgrade` removes it from pre-0.3.0 installs |
| 2026-07-26 | the illustration fell behind | ARCHITECTURE §1.2, §3.1, §9; `diagram.svg` (spec) | Doc-sync pass over README + `docs/`: the architecture illustration still drew the world of 2026-07-17. It showed `Onboarding` and `Importer` as separate infra capabilities (absorbed into `capability-lifecycle` at 0.3.0, §9), had no household band at all (§3.1's `upstream/` · `personal/` · `vendor/` · `.aos/` — so MOD.md was still captioned "co-located"), listed retired skill ids in the atoms tier, named `backups` in the bookkeeping line (superseded by `personal/` git history, see the 2026-07-25 location-doc row), and omitted Nanobot. `docs/diagram.svg` was **byte-identical to `spec:diagram.svg`**, which has not changed since the v0.1 spec commit, so both branches carried the same stale picture | **fixed on main**: redrawn on the household — a `Capability lifecycle` molecule replaces the two absorbed boxes, a household band sits between the kit tiers and the install band, the atoms tier uses installed names (`kb-route`, `gtd-quick-capture`, `capability-install`, …), the install band states pinned-render-plus-symlink and drops `backups`, and the harness row gains Nanobot. Verified by rendering, not by reading XML. **Spec side still owed**: `spec:diagram.svg` needs the same redraw (mechanical — main's file can be copied back); no spec-branch write happened in this pass. Text docs swept in the same commit (README household + render/symlink commitment, CONCEPTS household section + glossary, INSTALL context-block/`skill-creator`/host-feature wording, USAGE weekly-lint + build boundary, TESTING/CLAUDE lint counts 67/13 → 85/14, kb + gtd-capture support matrices) |
| 2026-07-27 | multi-user kb | design/kb-methodology.md §2, §4, §6.5, §7, §9; design/kb-authorization.md §4.5, §8 (spec c912d37) | **`log.md` was write-only.** §6.5 called it one of "two audit substrates" whose "grants audit cross-checks them"; the cross-check was never built, and `cli.py:878` exempted batched `auto-sync` commits *on the grounds that the log covered them* — so layer-2 enforcement checked nothing at all. It also guaranteed a conflict on every sync, because every verb appended to one file: that bit a **single user with one base on two machines**, before any sharing. User raised it directly ("we have git — why do we need log.md") | **spec amended (c912d37) + built this commit**: log.md deleted; every write verb makes its own commit, author = the human principal, committer = the acting agent, verb + path in `aos-verb`/`aos-path` trailers (trailers survive rebase and cherry-pick; `git notes` is not even pushed by default). New `base commit` attributes a hand-write, `base history` is the orientation read. The grants audit is real for the first time — nothing is batched, so only `bootstrap` is exempt. kb 0.5.1 → 0.6.0 |
| 2026-07-27 | multi-user kb | design/kb-methodology.md §2, §7 vs design/kb-authorization.md §7 (spec c912d37) | **The spec contradicted itself on shared state**: methodology §7 said "a shared base's state is the *team's* current-truth — that is a feature"; kb-authorization §7 said "two distinct authors touching it inside a lint window → violation". Both are true of one shared file and cannot both be satisfied by it. Same class as the log: `_ops/needs-review.md` was a single append target for every agent on every machine | **resolved by generalising the rule the kit already had**: "the inbox is a view, not a place" (one file per record) had been applied in exactly one place, and the three artifacts it was *not* applied to were the three that conflict. The review queue becomes `_ops/needs-review/<ts>-<slug>.md`; a shared base's state shards to `state/<principal>.yaml` (private bases keep the flat file, via the existing `Base.state_path()` seam). Each shard has one writer; the union is the team view. **No merge driver anywhere** — researched and rejected: `merge=union` swaps sides under rebase so line order scrambles, dedups only when hunk boundaries coincide, and forge-side merges ignore `.gitattributes` outright |
| 2026-07-27 | multi-user kb | design/kb-authorization.md §4.1; RFC-007 §8 Q2; new RFC-010 (spec c912d37) | **Nobody had a name.** `agent_subject()` was `$AOS_AGENT` with a default; `base init` *overwrote* the repo's git identity with `agents@localhost`; §4.1's subject forms have room for exactly one human (`user` = "**The human.** Root of all authority"). So a shared base could not express "Alice's agent may write here, Bob's may not", and could not say whose captures were whose | **built as evidence, not as a resolution**: git's own author/committer split (author = the human whose knowledge it is, committer = the acting agent — rebase preserves the author, forges show it in blame) plus a `BASE.yaml principals:` roster mapping author email → grants subject. **Absent a roster every write is `user`**, so private bases need no configuration and nothing changes for them. Recorded on kb-authorization §8 as Q0 and carried to **RFC-007 §8 Q2**, which already warned the answer "may change the table format" — RFC-010 explicitly does *not* resolve RFC-007 |
| 2026-07-27 | multi-user kb | ARCHITECTURE §4.3; design/kb-authorization.md §4.5 layer 2 | **Two live multi-user bugs.** (a) `cmd_inbox` walked `raw/**` with no filter, so every household's archiver drained everyone's captures — N× duplicate promotion, and one person's raw material entering another's agent context while that agent holds write access to shared knowledge (the "LLM scope violation" primitive). (b) `_do_capture`'s dedup scan was global and **printed the matching path**, so a second principal's identical capture was silently dropped and they were shown the first one's file path — the same bug class as mem0 #1805. Separately, `audience` was read only by `cmd_adopt`, so the entire shared-base safety property was skill prose | **fixed in artifacts**: `base inbox` scopes to the acting principal (`--all` is the designated-curator/CI path, and it reports a *count* of others' items, never a path); dedup is per-principal. Enforcement is by **routing method, not by refusing the verb** — a capture refusal would have contradicted ARCHITECTURE §4.3, kb-authorization §6.2 Case 3, `route` SKILL steps 1–2, and both v0.1 acceptance criteria, since §4.3 bars the *method*. So lint gained §4.5 layer 2's never-built check: **zero `method: llm` records in a shared base, ever**, plus the missing/invalid `kb_routing` and below-bar-but-routed checks |
| 2026-07-27 | multi-user kb | — (main-side defect found by research) | **`base sync` had a permanent-stall bug.** `git add -A` ran at the *head* of the loop, so on a conflicted worktree it staged the conflict-marker versions and committed them onto the detached rebase HEAD — and git refuses to start another operation over leftover state ("I am stopping in case you still have something valuable there"), so every later tick failed silently, forever. Also: five-minute crons fire on wall-clock boundaries, so N machines collide *systematically* rather than occasionally, and races scale ~N² | **fixed in artifacts**: `_sync_one` guards on sequencer state (`rebase-merge`, `rebase-apply`, `MERGE_HEAD`, `CHERRY_PICK_HEAD`, `REVERT_HEAD` via `git rev-parse --git-path` — `git status --porcelain=v2` does *not* report in-progress operations) **before** staging anything, and exits 5. Fast-forward first, merge only on genuine divergence (both tools that run unattended across many machines — Obsidian Git's default and git-annex — chose merge over rebase), jittered bounded retry on a lost push, and `sync --all` now reports the bases it skips instead of dropping them silently |
| 2026-07-27 | multi-user kb | ARCHITECTURE §4.4; RFC-010 Q1 (spec c912d37) | Nothing said **who curates a base several people write to**. Whichever household ran the checks was also the one whose agent read everybody else's raw material, and ARCHITECTURE §530's single-owner rule is enforced inside one household's lockfile — it cannot span households | **built (janitor) + deferred (curation)**: `capabilities/kb/adapters/github/` ships a workflow running `base lint --ci` (report-only stays the default; `--ci` returns a verdict). It needs no API key, which matters because **GitHub gates rulesets, branch protection and CODEOWNERS to Pro/Team/Enterprise for private repos** — on the plan a small team actually has, a failing check is the *only* enforcement that exists. `base init --audience shared` emits it, and a test asserts the emitted file is byte-identical to the adapter copy (anti-drift by test, not by discipline). **Promotion is explicitly TBD** (RFC-010 Q1) — external evidence is against automatic promotion (GovMem: "zero candidates are safe for automatic promotion" of 133; a public mem0 audit: 97.8% of 10,134 entries junk, one hallucination copied 808×). Today curation is per-principal by default or a designated curator, both expressible in the existing grants table, so no `curation:` field earns its place (rule of two) |
| 2026-07-27 | multi-user kb | design/kb-authorization.md (sweep owed); tests/golden/hermes/**; docs/diagram.svg | **Debt this pass deliberately did not pay.** (a) `kb-authorization.md` still describes `log.md` in a dozen normative passages (§1 component table, §2.5 line grammar, §3 sequence diagrams, §5 D/A table, §6 worked example) — half-sweeping a normative document is worse than naming the debt, so its header now states precisely what is owed. (b) The committed Hermes golden snapshots still carry the pre-change skill text; they are captured artifacts of a real run and must not be hand-edited — `check.mjs` passes because its expectations do not assert that text, but the snapshots are stale until a live e2e re-run. (c) `spec:diagram.svg` still owes main's redraw (carried from the 2026-07-26 row) | **recorded, not fixed**: (a) named in the kb-authorization header + RFC-010; (b) needs `tests/golden/PROTOCOL.md` re-run on a disposable `aos-test` Hermes profile, then `check.mjs --live full-install` + `normalize.mjs` — never simulated; (c) unchanged |
| 2026-07-27 | descope | `rfcs/RFC-010` §5 "decided (and already built)"; `design/kb-methodology.md` §9 `lint` row | The janitor shipped as a **decided** item while the question it half-answers — who curates a shared base — is RFC-010 Q1 and **open**. `base init --audience shared` emitted a workflow, which implies a shared base has a neutral actor today. It does not. The earlier row (2026-07-27, "multi-user kb") recorded it as built; that was reasonable — the deterministic half genuinely is free and needs no API key — but shipping the mechanical answer ahead of the judgment one made the kit claim more than it can back | **descoped in artifacts, amended on spec**: `capabilities/kb/adapters/github/**` deleted, `init --no-ci` and its janitor block removed, two tests deleted. **Everything the janitor ran stays** — the grants audit and the zero-`method: llm` check are `lint` checks and run anywhere. `lint --ci` survives with a new justification (a user's own hook needs a verdict; report-only stays the default and `--ci` is what makes that contract falsifiable). **The free-plan finding is kept as the reason CI was explored** — GitHub gates rulesets, branch protection and CODEOWNERS to Pro/Team/Enterprise for private repos, so a free-plan repo cannot block a bad push, only fail it. Spec: RFC-010 §5 moves out of "decided" into the open Q1 with the finding attached; `kb-methodology` §9's `lint` row drops "for an unattended janitor" |
| 2026-07-28 | kb capability surface | design/kb-methodology.md §2, §5, §9; ARCHITECTURE §2.1 (spec pass owed) | Every kb skill described a layout that no longer exists, and a find-replace would have left each one naming the right paths while still teaching the old model — a triage field where location is now the state, an `_ops/` directory that dissolved into `.kb/`, two lifecycle rules where there is one, growth stages nothing reads, a report file with no reader, and a `meta.gtd_triaged` marker no capability will write. Independently: three skills competed for the "capture" trigger space and three for "set up a KB", `[D]`/`[A]` notation appeared in half the skills, and `## Contents` blocks sat on reference files well under 100 lines | **rewritten on main, not swept**: six skills re-authored plus a new `capture` skill taking the general capture path over from gtd-capture (its two foreign dependencies cut — a gtd MOD key and a pointer into gtd's own reference). Enforced by `tools/check-kb-surface.mjs`, a per-capability retired-token gate written first and left failing (75 → 0), wired into **both** `tools/check.sh` and `.github/workflows/ci.yml` (the workflow duplicates the script rather than invoking it, so one edit would have left CI blind). `templates/AGENTS.md` 117 → 86 lines. **No per-file line budgets**: an earlier plan assigned each of 17 files its own ceiling, which is not a rule the Agent Skills authoring guide has — it sets one body limit (500 lines), one reference-TOC threshold (100), one description cap (1024), and aos-lint already enforces all three. `--write-report` deleted from the tool, not just from the prose: it committed a report nothing read. **Spec side owed**: `design/kb-methodology.md` §2/§5/§9 and the §8 wikilink rationale, which no document currently states |
| 2026-07-28 | capability docs directory | ARCHITECTURE §2.1 (package layout) | §2.1 enumerates a capability package's directories and has no place for developer-facing documentation. `README.md` is the repo-browser's summary and the skills are runtime artifacts; nothing held the Diátaxis *Explanation* and *Reference* material a developer needs to understand or look up the design without reading the spec branch | **built on main, spec amendment owed**: `capabilities/kb/docs/{design,reference}.md`. Explanation covers what a base is, the three pillars, the life of a capture, why one file per record, why git is the audit trail, expiry as the only lifecycle rule, principals and curation, and the tool/skills/agents/schedules division. Reference covers layout, frontmatter, the pending vocabulary, verbs, the query language, grants and exit codes — the exit-code table written from the code and verified by running each case, since a first draft had every row wrong. Main explains and the spec specifies, so no contract is restated — but §2.1 must name the directory |
| 2026-07-28 | curation, reversed | ARCHITECTURE §4.4; RFC-010 Q1; the 2026-07-27 row above that concluded otherwise | The earlier row concluded *"no `curation:` field earns its place (rule of two)"* — curation was expressible in the existing grants table, so a dedicated field had one consumer | **reversed, and recorded rather than overwritten**: the field earns its place now because two mechanical consumers exist. `kb inbox` scopes to the acting principal and `--all` is the designated-curator path, so the tool branches on the mode; and the shared-base review path branches on it a second time. (Corrected 2026-07-28: this row first named `kb pending list`, which has neither the scoping nor `--all` — the argument holds, but only `inbox` carries it.) Encoding it as a grants row instead would mean a row nothing reads *as a mode*, which is exactly the write-only artifact this redesign spent its effort deleting. `curation: self \| designated` plus `curator:` live in `.kb/base.yml`, and kb's ONBOARDING asks for it |
| 2026-07-28 | a base had no front door | ARCHITECTURE §2.1; design/kb-methodology.md §2 (tree) | The scaffolded tree explained itself to agents (`AGENTS.md`) and mapped its own content (`index.md`), but nothing addressed the **human** who opens the directory — despite "the store must survive the tooling" being the design's central claim. Separately, `BASE.yaml` and `state.yaml` had survived the layout move as git-tracked orphans beside their replacements, so the retired vocabulary was still shipping | **fixed on main**: `templates/base.README.md` renders to the base's `README.md`, and the two orphans are deleted. The template source is deliberately **not** named `README.md`: the template repo carries its own README describing itself, so a straight render would have copied "this repo exists to give kb init a materialization path" into every user's base — same rename shape as the existing `gitattributes` → `.gitattributes`. Verified against the live template repo end to end, not only via `--templates`, and `AlmogBaku/aos-kb-template` was pushed in sync. **An anti-drift check now exists** — `tools/check-template-drift.mjs`, added after the gap bit twice in one session: the first push happened before `templates/AGENTS.md` was trimmed, so the clone path shipped the 117-line contract while the kit had 87, and no test could see it (every test passes `--templates`, so nothing in the suite reads the repo). The check is deliberately **not** in `tools/check.sh` or CI, because it needs the network and a gate that fails offline is a gate people learn to skip; run it after touching `templates/`. It reads `TEMPLATE_REPO_URL` from the tool rather than duplicating it |
| 2026-07-29 | capability review as architecture | ARCHITECTURE §2.5, §5.2, §7 (spec pass owed); `design/capability-anatomy.md` | Nothing in the kit reviewed a capability **as a system**. `capability-build`'s Design stage asked for a feature list — scope, depends, skills/agents, acceptance, open items — and never for a flow, a decision tree, a component graph or failure modes, so a capability could be designed, approved, built and shipped without anyone drawing how it works. `build.md`'s entire verification was `aos-lint --root personal` plus "show the file tree", which validates *schema* and structurally cannot check whether the prose describes a tool that behaves that way — the defect class holding **27 of this ledger's 65 rows**. The anatomy exhibit documents layout ("file by file", "where each declared thing lands"), not architecture, and `capabilities/kb/docs/design.md` was the only file in any capability that drew its own | **built on main**: `capability-lifecycle` 0.3.2 → 0.3.3 with a tenth skill, `review`, installing as `capability-review`. Part 1 is architecture (trace flows + the decision points prose leaves implicit, map business-logic units separately from the resources powering them, mermaid diagram once there is more than one moving part, adversarially stress the tree, classify failure modes by how they surface); Part 2 is the textual sweep. `build/reference/design.md` now demands the architecture so review verifies a design rather than reverse-engineering one, and `build.md` points at review before the linter. **Validated by dogfooding on kb (7 skills) and capability-lifecycle (10)**: both runs independently reported steps 1–5 producing every high-severity finding while the command sweep produced none, and both named the same eight weaknesses in the skill, all fixed. `tessl review run quality`: 100%. **Spec side owed**: §2.5 gains the skill, §5.2 a fifth harness sheet, §7 nothing about Claude Code as a target, and `capability-anatomy.md` should include a capability's flow, not only its file tree |
| 2026-07-29 | `aos-lock render` destroyed user-authored source | — (main-side defect, found by the new review skill on its first run) | **Data loss, and it fired on the user-authored path only.** `cmd_render` did `shutil.rmtree(dest)` then `shutil.copytree(src, dest)` with no check that `dest` sits inside `src`. For an upstream capability those are different roots and the code is correct; for a capability the user authored they are the **same directory** — `capability-build` writes the package to `personal/capabilities/<id>/`, and both `install` step 7 and `upgrade` step 3c render with `--out personal/capabilities/<id>/skills`. Reproduced: the hand-written `SKILL.md` and its whole `reference/` tree are deleted, then `copytree` dies with an unhandled `FileNotFoundError` on the source it just removed. `upgrade` is the caller that passes `--force`, so it fires on the **first upgrade** of anything `capability-build` or `capability-import` produced. Invisible to CI, which lints `upstream/` only | **fixed on main** with a regression test proven to fail without the guard: `render` now refuses before touching anything when source and destination overlap in either direction. Found by step 2 (units vs resources) and step 3 (drawing the graph) — two reviewers each reported reading the two instructions attentively *without* seeing the collapse, because in prose "render into `personal/.../skills`" and "the package lives in `personal/capabilities/<id>/`" are two unrelated sentences; as two nodes they are one node. This is the evidence for the architecture-first ordering |
| 2026-07-29 | Claude Code as a harness | ARCHITECTURE §5.2, §5.3, §7 (spec pass owed) | Claude Code was listed "📋 no sheet yet" in four support matrices while being a first-class target for these skills. §5.3 already guarantees "one identity end to end" — the install writes the prefixed name into both the render directory and the frontmatter — so nothing structural was missing, only the sheet. Considered and **rejected**: pre-naming the source `capability-review` so a symlink would satisfy Claude Code's dir==name convention. §2.1 is explicit that the installed name is "computed, never authored", and §2.2 says `skill_prefix` exists precisely because this capability's skills' ids "would otherwise repeat their capability" — so pre-naming would have retired the field's own rationale to save one file | **built on main**: `reference/harness-claude-code.md`, six §5.2 sections, research-drafted (no e2e). Two claims in the first draft were **false and caught in review before anyone trusted them**: "this harness has no scheduler" (`CronCreate`/`CronList`/`CronDelete` exist — the truth is subtler and worse to get wrong: `durable: true` or the job dies with the session, recurring jobs auto-expire after 7 days, and they fire only while the REPL is idle), and an unverified assertion that per-agent skill scoping does not exist (`tools:` restriction is confirmed; skills scoping is not, so the sheet now says so). Also: `CLAUDE.md` → `AGENTS.md` with `CLAUDE.md` a committed symlink (mode 120000), since a repo about multi-harness support carried its guidance only in the filename one harness reads |
| 2026-07-29 | kb: the sync sweep steals attribution | design/kb-methodology.md §6.5; design/kb-authorization.md §4.5 (spec + main fix owed — **NOT fixed here**) | Found by the review skill's step 4 ("two paths writing the same state with no ordering between them") while dogfooding on kb. The archiver's promote step writes a wiki page with ordinary file tools and *then* runs `kb commit --verb promote` to attribute it — but the `sync` exec cron runs every 5 minutes, does `git add -A`, and commits anything uncommitted as `sweep: N paths written outside a verb`. Reproduced on a `rebase-5min` base: the page lands committed as `agent:main`, the archiver's `kb commit` then fails `nothing to commit — those paths are unchanged`, and the next weekly lint reports a grants-audit **critical** against an agent that followed its prompt exactly. The write→attribute window is a multi-second gap on every promoted page and every mechanical import batch; no document declares an ordering between the two paths. Exit-0-looking success with a permanently wrong record and no recovery verb | **recorded, not fixed** — kb is Plan 02's surface and this is a design question, not a typo: either `sync` learns to leave unattributed paths alone (and something else must then surface them), or promote attributes before it writes, or the two acquire a lock. Naming it here rather than patching another plan's capability mid-flight. Same dogfood also found `failed:` documented in three places and raised as a lint critical with **no layout-2 writer**, `curation: designated` promising wiki grants it never seeds, and `kb state check` reading only the acting principal's shard so the archiver's eviction pass reports clean on everyone else's stale items |
