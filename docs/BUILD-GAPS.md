# Build-gap ledger

Append-only. Every mismatch the build phase finds between an artifact and the spec gets a row.
The spec fix lands **in the same commit** as the artifact that revealed the gap (firm-position
discipline, ARCHITECTURE §8). Anything in an open RFC's territory is appended to that RFC as
evidence — never quietly fixed in ARCHITECTURE. A proposed new frontmatter field must name its
two in-repo machine consumers (rule of two) or stay prose.

| Date | WP | Spec section | Mismatch | Resolution |
|---|---|---|---|---|
| 2026-07-22 | WP3 (pre-seeded) | design/kb-methodology.md §8 vs ARCHITECTURE §2.1 | `archiver.agent.yaml` placed inside the methodology dir by kb-methodology §8, but §2.1 and kb-authorization put agent specs in `agents/` (where `schedules[].agent` resolves) | **fixed in kb-methodology §8**: canonical in `agents/`, prompts in `methodologies/*/archiver/` |
| 2026-07-22 | WP3 (pre-seeded) | ARCHITECTURE §2.1 / §3.1 | ONBOARDING.md question `type` vocabulary is never enumerated anywhere | **resolved in artifacts**: `string\|number\|boolean\|enum\|list\|path` + orthogonal `secret` (lint enforces; normative text = onboarding interview skill) |
| 2026-07-22 | WP4 | ARCHITECTURE §2.2 vs design/capability-anatomy.md | §2.2's example `prompt_ref: skills/capture/drain-prompt.md` contradicts the anatomy exhibit (`skills/drain/drain-prompt.md` — the prompt belongs to the drain skill) | **fixed in §2.2** (anatomy wins) |
| 2026-07-22 | WP4 | capabilities/kb + gtd-capture | Both kb `nightly-promote` and gtd `nightly-drain` defaulted to 23:00 on one inbox — undefined order | **resolved in artifacts**: drain triages at 23:00 and only marks `#triaged`; promote files + empties at 23:30. Ordering documented in both manifests |
| 2026-07-22 | WP3 | ARCHITECTURE §2.2 | Methodology-shipped skills (karpathy-3layer `lint/`) can't appear in manifest `skills[]` (path fixed to `skills/<id>/`), so they have no `used_by` declaration | **documented in kb-methodology §8**: the methodology contract carries them; they materialize with the archiver. Rule-of-two: no new manifest field |
| 2026-07-22 | WP6 (pre-seeded) | ARCHITECTURE §2.1 | Is `MOD.example.md` required when a capability has no ONBOARDING.md (importer)? | **resolved in artifacts**: presence-paired — required iff ONBOARDING.md exists (lint `manifest/mod-example`); importer ships neither |
| 2026-07-22 | WP5 (routing replay) | design/kb-authorization.md §2.1 / ARCHITECTURE §4.2 | "Explicit tag wins" but nothing stores the tag→KB mapping ("work:" names no KB literally) — step 1 was under-determined | **resolved in artifacts**: registry entries carry `tag:` alias (init skill shape + fixture); route skill matches prefix against `tag`/`name`. Registry shape is RFC-006 input — noted there via this row |
| 2026-07-22 | WP2 | ARCHITECTURE §5.3 | Hermes agent row claimed "profile + entry in config.yaml" — profiles are directory-defined; no config.yaml registry exists | **fixed in §5.3** (same commit as the Hermes cheatsheet); create via `hermes profile create` |
| 2026-07-22 | WP2 | ARCHITECTURE §5.3 | Hermes schedule row said write `jobs.json` "tagged `origin: aos:<cap>@<ver>`" — jobs.json is scheduler-owned (live state, schema drift) and its `origin` field already means chat provenance | **fixed in §5.3**: `hermes cron create` + `aos:<cap>:<id>` name prefix + lockfile job id |
| 2026-07-22 | WP2 | ARCHITECTURE §3.1, §5.3 | Secret store named `auth.json`/`hermes-auth` — auth.json is Hermes's provider-credential state; capability secret values live in `.env` | **fixed** (§3.1 example → `{store: hermes-env, key: VAR}`; §5.3 secret row) |
| 2026-07-22 | post-build review | ARCHITECTURE §5.2 | Host vocabulary said `scheduler`; every harness's actual noun is `cron` (hermes cron, openclaw cron, Claude Code CronCreate, opencode-cron plugins — researched) | **renamed to `cron`** on spec branch + all main artifacts (lint vocabulary, manifests, cheat-sheet, BOOTSTRAP) |
| 2026-07-22 | post-build review | ARCHITECTURE §4.4 / design/kb-methodology.md | Methodology named `karpathy-3layer` — an invented label; the pattern's public name is karpathy's llm-wiki (gist 442a6bf) | **renamed `karpathy-llm-wiki`** on both branches (dir, manifests, registry values, spec docs); ledger keeps historical rows verbatim |
| 2026-07-23 | kb redesign | ARCHITECTURE §2, §4.4 (spec c8ef7a3) | kb redesigned around the base engine (store/curation/state): methodology seam dissolved, entry-skill convention, `base` tool as canonical [D] executor, exec schedules, BASE.yaml, current-truth doctrine, inbox-as-view, state.yaml. Spec amended first (firm-position discipline); artifacts rebuilt against it | **this commit**: capability tree v2 (skills kb/route/recall/init/adopt; agents/archiver + prompts; templates; tool), lint dual-anatomy, expectations updated. Other capabilities NOT migrated (deliberate — `_migrate.md` local ledger; lint tolerates legacy with warning) |
| 2026-07-23 | kb redesign | ARCHITECTURE §2.2 | Schedules could not express script-direct crons — kb-sync woke an LLM every 5 min to run a shell script | **spec + artifacts**: `schedules[].exec` (xor agent+prompt_ref), lint-enforced; kb `sync` schedule is exec-type; cheat-sheet materialization documented |
| 2026-07-23 | kb redesign | design/kb-methodology.md §4 vs §5.3 | Spec's universal frontmatter block omitted `growth_stage` while §5.3 keeps growth stages | **resolved in artifacts** (optional universal field, tool-linted when present); spec §4 should list it as optional — noted for next spec touch |
| 2026-07-23 | kb redesign | RFC-009 (opened) | Cross-capability skill dependency has no vocabulary (`used_by` can't cross capabilities); gtd-capture→kb composes only via shared main agent | **RFC-009 opened on spec branch**; deferred to its own session per user |
| 2026-07-23 | kb redesign | capabilities/gtd-capture | gtd drain reads `ops/inbox.md`, which the redesigned base no longer has (inbox = pending view over raw/captures/) | **deferred to gtd-capture migration** (`_migrate.md` row): drain consumes `base inbox`; zone grant becomes `route-into raw/captures/**` |
| 2026-07-24 | import + LFS | design/kb-methodology.md §2, §6.7, §9 (spec 249596b, amended same-day) | Import initially specced/built with a deterministic apply engine (plan DSL + transform queue); user correctly flagged it as over-engineered — transform-on-import routes every page through agent judgment anyway, so the engine served the rejected lift-and-place mode | **reversed in artifacts + spec amended**: import is an AGENT procedure (skill + agreement file + progress checklist + subagent batches); the tool keeps exactly one import verb, `survey`. LFS landed as standalone base plumbing (gitattributes template, init wiring, lint check) |
