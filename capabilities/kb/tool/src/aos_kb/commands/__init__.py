"""Every verb, grouped by pillar: lifecycle (init/adopt/migrate — the verbs that
create or convert a base's on-disk identity), capture (the `.kb/pending/` pipeline
and its read-side counterpart), wiki (page content), lint (alone, for its size),
sync (alone, for its subprocess/retry/timing logic), admin (thin cross-cutting
verbs), survey (import survey's one nested subcommand)."""
