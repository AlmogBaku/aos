# next-actions

> Standalone next-actions only — no owning project (those live in the project page's
> `next_action` frontmatter instead). One entry per action, phrased per the user's
> `action_format` answer. Filed and drained nightly by gtd-capture's drainer (~23:00,
> before kb's own archiver promote at ~23:30). Never hand-edited by the user directly —
> fire a capture and let the next drain pass pick it up.
