"""Every verb, grouped by what it acts on: inspect (manifest/skills/home — read a
package or the household and report), render (the one verb that materializes a skill),
lockfile (the seven verbs that own <home>/.aos/installs.lock.yaml)."""
